## Type de variable

Faites-moi compiler !

<div class="hint">
  Le message du compilateur indique que Rust ne peut pas déduire le type que la variable <code>x</code> a avec ce qui est donné ici.

  Que se passe-t-il si vous annotez la ligne 2 avec une annotation de type ?

  Que se passe-t-il si vous donnez une valeur à `x` ?

  Que se passe-t-il si vous faites les deux ?

  Quel type devrait avoir `x`, de toute façon ?

  Et si `x` est du même type que 10 ? Et si c'est un type différent ?
</div>