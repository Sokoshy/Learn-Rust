## Tranche de chaîne

Faites-moi compiler sans modifier la signature de la fonction !

<div class="hint">
  Oui, ce serait vraiment facile de résoudre cela en changeant simplement la valeur liée à <code>word</code> pour qu'elle soit une tranche de chaîne au lieu d'un <code>String</code>, n'est-ce pas ?

  Il y a cependant un moyen d'ajouter un caractère à la ligne 3 qui transformera le `String` en une tranche de chaîne.
</div>