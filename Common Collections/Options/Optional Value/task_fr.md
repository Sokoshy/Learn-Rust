## Options dans le filtrage par motif

Faites en sorte que le code compile ! Consultez les indices si vous êtes bloqué.

<div class="hint">Consultez ces liens :
<a href="https://doc.rust-lang.org/rust-by-example/flow_control/if_let.html">1</a>, 
<a href="https://doc.rust-lang.org/rust-by-example/flow_control/while_let.html">2</a>.</div>

<div class="hint">Rappelez-vous que les Options peuvent être empilées dans <code>if let</code> et <code>while let</code>.
Par exemple : <code>Some(Some(variable)) = variable2</code>.
Voyez aussi <code>Option::flatten</code>.</div>