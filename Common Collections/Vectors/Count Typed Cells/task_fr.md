## Compter les cellules typées

Nous avons un vecteur de valeurs `SpreadsheetCell`. Votre tâche est de compter les cellules pour chaque type.
Il y a la `struct CellsCounter` pour stocker les résultats.

<div class="hint">Sur quoi devrions-nous itérer ?</div>

<div class="hint">L'IDE peut aider à générer tous les cas dans le <code>match</code>. Il suffit de demander !</div>