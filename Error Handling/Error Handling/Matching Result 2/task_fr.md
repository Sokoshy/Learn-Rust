## Résultat de correspondance étendu

Voici un programme qui tente d'utiliser une version complète de la fonction `total_cost` de l'exercice précédent. Cependant, cela ne fonctionne pas -- nous ne pouvons pas utiliser l'opérateur `?` dans la fonction `main()` ! Pourquoi pas ?

<div class="hint">
Si d'autres fonctions peuvent retourner un <code>Result</code>, pourquoi <code>main</code> ne le pourrait-elle pas ?
</div>