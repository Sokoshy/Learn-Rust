## Entier non nul positif

Faites passer le test !

<div class="hint">
<code>PositiveNonzeroInteger::new</code> crée toujours une nouvelle instance et renvoie un résultat <code>Ok</code>. Il devrait effectuer des vérifications, renvoyant un résultat <code>Err</code> si ces vérifications échouent, et ne renvoyant un résultat <code>Ok</code> que si ces vérifications déterminent que tout est... correct :)
</div>