## Tests de multiplication par deux

Nous avons la fonction `times_two` dans `src/lib.rs` et des tests d'intégration dans `tests/tests.rs`.  
Les tests ne vérifient pas notre fonction — faites en sorte qu'ils le fassent de manière à ce que les tests réussissent. Ensuite, écrivez un second test qui vérifie que nous obtenons le résultat attendu lorsque nous appelons `times_two` avec un nombre négatif.

Pas d'indices, vous pouvez le faire :)