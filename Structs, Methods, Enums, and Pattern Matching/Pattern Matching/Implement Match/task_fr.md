## Implémenter la correspondance

Adressez tous les TODO pour faire passer les tests !

À la fin, la `couleur` de la structure `état` devrait être `(255, 0, 255)` 
et sa `position` –– `{ x: 10, y: 15 }`. 
Le message final doit être `Quitter`.