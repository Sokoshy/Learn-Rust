## Liaison de motif

Faites en sorte que le code se compile en lisant le message d'erreur et la suggestion pour le corriger. Consultez les indices si vous êtes bloqué.

<div class="hint">Le compilateur indique qu'un déplacement partiel a eu lieu dans l'instruction <code>match</code>. Comment cela peut-il être évité ?</div>

<div class="hint">Le compilateur montre la correction nécessaire, c'est le mot-clé <code>ref</code>. Après avoir effectué la correction suggérée par le compilateur, lisez : <a href="https://doc.rust-lang.org/std/keyword.ref.html">https://doc.rust-lang.org/std/keyword.ref.html</a>.</div>