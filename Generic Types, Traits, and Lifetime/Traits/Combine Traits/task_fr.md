## Combiner des traits

Votre tâche consiste à remplacer les sections `??` afin que le code se compile.
Ne modifiez aucune ligne autre que celle indiquée.

<div class="hint">
Pour s'assurer qu'un paramètre implémente plusieurs traits, utilisez la <code>syntaxe +</code>. Essayez de remplacer le
<code>??</code> par <code>impl <> + <></code>.

En savoir plus [ici](course://Generic+Types,+Traits,+and+Lifetime/Traits/Traits as Parameters).
</div>